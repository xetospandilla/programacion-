#include <iostream>
#include <vector>
using namespace std;

double mediaCasillas(vector <double> arr, unsigned int p, unsigned int f){
    double sum;
    for(unsigned int i = 0; p < i < f; i++){
        sum =+ arr[i];
    } double res = sum / arr.size();
    return res;
}